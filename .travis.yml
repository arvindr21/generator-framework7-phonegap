language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-framework7-phonegap' ]; then cd .. && eval "mv $currentfolder generator-framework7-phonegap" && cd generator-framework7-phonegap; fi

